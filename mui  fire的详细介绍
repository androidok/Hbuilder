 //触发详情页面的newsId事件
  mui.fire(detailPage,'newsId',{
    id:id
  });
  
  第一个参数表示的是我们要向谁传递数据，也就是我们要向哪一个页面传递数据，第二个参数是我们自定义的一个事件的名称，例如是点击事件tap还是其他的事件，这个事件
  使我们自己自定义的，主要是为了在detailPage页面中监听处理这个事件，第三个参数就是我们需要传递的参数，
  在这里fire的主要作用就是处理要传递的参数的目标页和要处理的事件和传递的参数数据
  
  
  对第二个参数定义的事件，我们可以在目标页中监听处理这个事件
